Min-Makespan-Distanciel

Notre application utilise Maven ainsi que Java 11 (obligatoire).
Pour la lancer, se rendre dans le répertoire et exécuter cette commande : mvn clean javafx:run.
Il n'y a pas de fichier exécutable dans cette archive car l'interface est lancée directement par Maven lors de la compilation.
